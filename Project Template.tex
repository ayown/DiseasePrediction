\documentclass[12pt,a4paper]{article}
\usepackage[a4paper,top=1in,bottom=1in,left=1in,right=1in]{geometry}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{setspace}
\usepackage{booktabs}
\usepackage{array}
\usepackage{amsmath}
\PassOptionsToPackage{hyphens}{url}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{caption}
\usepackage{float}
\usepackage{enumitem}
\usepackage{parskip}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning}

\hypersetup{
    colorlinks=true,
    linkcolor=teal,
    urlcolor=blue!70!black,
    citecolor=teal
}

\lstset{
    language=Python,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue!70!black}\bfseries,
    commentstyle=\color{gray},
    stringstyle=\color{red!60!black},
    backgroundcolor=\color{gray!8},
    frame=single,
    rulecolor=\color{gray!40},
    breaklines=true,
    showstringspaces=false,
    numbers=left,
    numberstyle=\tiny\color{gray},
    numbersep=6pt,
    xleftmargin=15pt
}

\begin{document}

% ---------------- COVER PAGE ----------------
\begin{center}

% -------- LOGO --------
\includegraphics[width=4.5cm]{LOGO.pdf}\\[0.5cm]

% -------- UNIVERSITY NAME --------
{\Large \textbf{SIKSHA 'O' ANUSANDHAN}}\\
{\small (Deemed To Be University)}\\
{\small \textcolor{blue!60!black}{Accredited (3rd Cycle) by NAAC with A++ Grade}}\\[0.5cm]

\textcolor{gray}{\rule{12cm}{0.4pt}}\\[0.5cm]

Admission Batch: \textbf{2023} \hspace{1cm} Session: \textbf{2025--26}\\[1cm]

% -------- TITLE BLOCK --------
{\Large \textbf{\textcolor{teal}{Major Project Report}}}\\[0.3cm]

{\large \textbf{of}}\\[0.3cm]

{\large \textbf{\textcolor{purple}{MACHINE LEARNING CONCEPT -2 (CSE 3968)}}}\\[0.8cm]

{\large \textbf{On}}\\[0.3cm]

{\Huge \textbf{\textcolor{red!70!black}{Disease Prediction System\\[0.2cm]Using Machine Learning}}}\\[1cm]

% -------- STUDENTS --------
\textbf{\large Submitted by:}\\[0.5cm]

\begin{tabular}{ll}
1) NAME 1 & 2341\_\_\_\_\_ \\
2) NAME 2 & 2341\_\_\_\_\_ \\
3) NAME 3 & 2341\_\_\_\_\_ \\
4) NAME 4 & 2341\_\_\_\_\_ \\
\end{tabular}

\vspace{1cm}

Section: \textbf{2341\_\_\_} \hspace{0.8cm}
Semester: \textbf{\_\_\_\_\_\_} \hspace{0.8cm}
Branch: \textbf{\_\_\_\_\_\_}

\vspace{1.2cm}

% -------- FOOTER --------
{\small
Department of Computer Science \& Engineering\\
\textbf{Institute of Technical Education and Research (ITER), Siksha `O' Anusandhan Deemed to be University}\\
Jagamohan Nagar, Jagamara, Bhubaneswar, Odisha -- 751030
}

\vfill

\textbf{(May, 2026)}

\end{center}

\newpage

% ---------------- DECLARATION ----------------
\newpage
\begin{center}

{\Large \textbf{DECLARATION}}\\[1cm]

\end{center}

\noindent
We hereby declare that the project report entitled

\begin{center}
\textbf{``Project title''}
\end{center}

\noindent
is a bonafide record of the work carried out by us during the 6\textsuperscript{th} semester of the Bachelor of Technology program in Computer Science and Engineering for the course \textbf{Machine Learning Concept - 2 (CSE 3968)}, under the regulations of Siksha `O' Anusandhan Deemed to be University.

\vspace{0.5cm}

\noindent
This work has been carried out under the guidance of the project supervisor and has not been submitted elsewhere for any academic evaluation or certification. We further declare that there are \textbf{no competing interests} associated with this project.

\vspace{1cm}

\noindent
\textbf{Submitted by:}

\vspace{0.5cm}

\begin{tabular}{p{6cm} p{6cm}}
NAME 1 (Regd. No: 2341\_\_\_\_\_) & Signature: \rule{5cm}{0.4pt} \\
\\
NAME 2 (Regd. No: 2341\_\_\_\_\_) & Signature: \rule{5cm}{0.4pt} \\
\\
NAME 3 (Regd. No: 2341\_\_\_\_\_) & Signature: \rule{5cm}{0.4pt} \\
\\
NAME 4 (Regd. No: 2341\_\_\_\_\_) & Signature: \rule{5cm}{0.4pt} \\
\end{tabular}

\vspace{1.5cm}

\noindent
\textbf{Project Supervisor:} \\
\vspace{0.5cm}
Name: \rule{8cm}{0.4pt} \\
\vspace{0.3cm}
Signature: \rule{6cm}{0.4pt}

\vfill

\begin{center}
Department of Computer Science \& Engineering\\
Institute of Technical Education and Research (ITER)\\
Siksha `O' Anusandhan Deemed to be University\\
Bhubaneswar, Odisha\\[0.5cm]

\textbf{Date:} \rule{4cm}{0.4pt}
\end{center}

\newpage

% ---------------- TOC ----------------
\tableofcontents
\newpage

% ================================================================
\section{Abstract}
% ================================================================

Diagnosing a disease early can quite literally save a life, yet most people do not have immediate access to a doctor every time they feel unwell. This project tries to address that gap. We built a \textbf{Disease Prediction System} that takes a patient's self-reported symptoms --- encoded by how severe each symptom is --- and predicts the most likely disease from a pool of 41 possibilities.

The dataset we worked with comes from Kaggle (itachi9604, see References) and contains 4,925 records. Instead of treating every symptom as a simple yes/no, we assigned each one a severity weight between 1 and 7, which gives the model a much more nuanced picture of the patient's condition. Three classifiers were trained and evaluated: a \textbf{Decision Tree}, a \textbf{Random Forest}, and \textbf{XGBoost}. Random Forest came out on top with \textbf{91.88\%} accuracy on unseen test data. XGBoost followed closely at \textbf{91.07\%}, while the Decision Tree, despite being the simplest of the three, still managed a respectable \textbf{87.72\%}.

\textbf{Keywords:} Disease Prediction, Machine Learning, Random Forest, XGBoost, Decision Tree, Symptom Severity, Multi-class Classification.

% ================================================================
\section{Introduction}
% ================================================================

Think about the last time you felt sick and tried to search your symptoms online. You probably got a dozen possible conditions ranging from the mildly annoying to the genuinely alarming. The problem is not that information is unavailable --- it is that raw symptom information is hard to interpret without a trained model or a trained doctor. Machine learning offers a middle ground.

Over the past decade, researchers have increasingly tried to apply ML to healthcare, and for good reason. Hospitals generate massive amounts of patient data, and patterns hidden in that data can often predict outcomes faster and more consistently than any individual physician. But most of this work stays in research labs. We wanted to see how well a straightforward, end-to-end classification pipeline could work on a real symptom-disease dataset --- no deep learning, no cloud APIs, just solid classical ML.

For this project, we chose three algorithms that represent a natural progression in complexity:

\begin{itemize}
    \item \textbf{Decision Tree} --- easy to understand, easy to explain, and a good starting benchmark.
    \item \textbf{Random Forest} --- takes the idea of a single tree and builds an entire forest of them, averaging out the errors.
    \item \textbf{XGBoost} --- goes one step further by building trees sequentially, each one learning from the mistakes of the last.
\end{itemize}

The input to the system is a vector of symptom severity scores, and the output is a predicted disease label, chosen from 41 possible conditions.

% ================================================================
\section{Problem Statement}
% ================================================================

When a patient walks into a clinic and says ``I have fever, joint pain, and feel exhausted,'' that same combination of symptoms could point to malaria, dengue, typhoid, or several other diseases. Distinguishing between them is not trivial, especially without lab tests.

\textbf{The problem we are solving:} Given the severity of symptoms reported by a patient, can a machine learning model reliably predict which of 41 diseases they are most likely suffering from?

\begin{itemize}
    \item \textbf{Input:} A 136-dimensional feature vector where each dimension represents one symptom; the value is 0 if the symptom is absent, or a weight from 1 to 7 reflecting its clinical severity if present.
    \item \textbf{Output:} One of 41 disease labels (e.g., Malaria, Tuberculosis, Diabetes, etc.).
    \item \textbf{Task type:} Supervised multi-class classification.
\end{itemize}

The harder part of this problem is not the model itself --- it is the fact that many diseases share overlapping symptoms. Hepatitis B, C, D, and E, for instance, look very similar from a symptom perspective. Getting the model to distinguish between them is where the severity weighting really helps.

% ================================================================
\section{Objectives}
% ================================================================

Going into this project, we had a few clear goals in mind:

\begin{enumerate}
    \item Properly clean and prepare the raw symptom dataset so it is actually usable by a classifier.
    \item Build three different models and train them fairly, using the same data split and preprocessing.
    \item Evaluate each model rigorously --- not just accuracy, but also precision, recall, F1-score, and a proper confusion matrix.
    \item Figure out which model is actually worth recommending for a real-world deployment scenario.
    \item Understand \textit{which symptoms matter most} by analysing feature importances from the Random Forest.
\end{enumerate}

% ================================================================
\section{Literature Review}
% ================================================================

Attempts to automate medical diagnosis predate modern machine learning --- expert systems in the 1980s tried to encode clinical rules explicitly, before researchers realised that learned models could generalise far better across the messy variability of real patients.

\textbf{Early symptom-based approaches} leaned heavily on hand-crafted rule bases, but those systems broke down whenever a patient presented an unusual combination of symptoms not covered by the rules. Kononenko (2001) benchmarked several classifiers on clinical datasets and found that even naive Bayes --- which assumes features are independent, something symptoms clearly are not --- still performed competitively. That result is instructive: if a model with such a strong wrong assumption can do well, a properly-tuned tree ensemble working with severity-weighted features should do considerably better. Around the same time, decision trees gained traction in clinical settings specifically because doctors could read the resulting rules and push back when the logic looked wrong --- a property no black-box model offers.

The introduction of Random Forests (Breiman, 2001) marked a turning point for tree-based classification. Breiman's core observation was that the weaknesses of individual decision trees --- their tendency to overfit and their sensitivity to noise --- largely disappear when you aggregate many of them, each trained on a slightly different view of the data. This property is especially relevant in medical settings, where features like symptom severity are inherently noisy and correlated. Subsequent healthcare studies, including Rajpurkar et al.\ (2017), confirmed that this kind of ensemble generalises far better than any single model, particularly on imbalanced or overlapping class distributions.

A different line of tree-based research led to gradient boosting. Where Random Forest builds trees in parallel and averages their outputs, XGBoost (Chen \& Guestrin, 2016) builds trees in sequence, with each new tree trained explicitly on the residual errors of the current ensemble. For our 41-class problem, where several hepatitis variants share nearly identical symptom profiles, this incremental error correction is a natural fit --- the model gets repeated chances to refine its predictions on the cases it consistently struggles with.

\textbf{Where existing work falls short} is mostly in scope. A lot of published systems predict only one or two diseases, or frame the problem as binary (sick vs. healthy). Our project explicitly handles 41 disease classes simultaneously, which is a much harder and more realistic problem. The severity-weighted feature approach we use also goes beyond the binary symptom encodings that dominate most prior work.

% ================================================================
\section{Dataset Description}
% ================================================================

\subsection{Source}
We used the dataset published on Kaggle by \textit{itachi9604} (full citation in References), accessed through the GitHub mirror by \textit{amMistic}. The data is spread across two files:

\begin{itemize}
    \item \texttt{disease\_raw.csv} --- 4,925 patient records, each listing up to 17 symptoms by name.
    \item \texttt{symptom\_severity.csv} --- a lookup table mapping each of the 133 known symptoms to a severity weight between 1 and 7.
\end{itemize}

\subsection{Dataset Characteristics}

\begin{table}[H]
\centering
\caption{Dataset Summary}
\begin{tabular}{ll}
\toprule
\textbf{Property} & \textbf{Details} \\
\midrule
Total Records       & 4,925 \\
Disease Classes     & 41 \\
Symptom Features    & 136 (after vectorisation) \\
Feature Type        & Severity-weighted (0 = absent, 1--7 = present) \\
Missing Values      & 0 (after preprocessing) \\
Train / Test Split  & 80\% / 20\% (stratified) \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Feature Representation}
The raw CSV stores each patient's symptoms as up to 17 text strings spread across separate columns (\texttt{Symptom\_1} through \texttt{Symptom\_17}). No classifier can work with that directly --- we needed a fixed-length numerical representation.

Our approach: gather every unique symptom name that appears anywhere in the dataset (133 in total), then build a 136-column matrix where each column corresponds to one symptom. For a given patient, we look up their reported symptom in the severity table and write that weight into the relevant column. Unreported symptoms stay as 0.

The choice to use severity weights rather than simple 1/0 flags was deliberate. Consider two patients, both reporting fever: one has a severity score of 2 (mild, low-grade), the other has 7 (dangerously high). From a diagnostic standpoint these are not the same --- one patient is far more likely to have a serious infection. Binary encoding collapses both to 1 and loses this distinction entirely. Severity encoding keeps it, and we believe this is one of the main reasons our models reached above-85\% accuracy on a 41-class problem without any deep learning.

% ================================================================
\section{Methodology}
% ================================================================

\subsection{Data Collection}
Both dataset files were downloaded from their public GitHub URLs using \texttt{curl}. No API keys or login credentials were needed. The files were stored locally in a \texttt{dataset/} subfolder within the project directory.

\subsection{Data Preprocessing}

Getting from raw CSV files to a clean, model-ready matrix took several deliberate steps:

\begin{enumerate}
    \item \textbf{Severity-Weighted Feature Construction:} We iterated through every symptom column for every patient, looked up the severity weight for each named symptom, and filled the corresponding position in a 136-column feature matrix. Anything not reported stayed 0.

    \item \textbf{Incomplete Symptom Simulation:} Real patients do not always report every symptom they have. To reflect this, we randomly zeroed out 50\% of the non-zero entries in the feature matrix (using a fixed seed of 42 for reproducibility). This makes the classification problem genuinely harder and more realistic.

    \item \textbf{Label Encoding:} The target column (\texttt{prognosis}) contains disease names as strings. We used \texttt{LabelEncoder} to convert these to integer indices --- a requirement for all three classifiers.

    \item \textbf{Train-Test Split:} We split the data 80/20 with stratification, so each disease class appears proportionally in both sets. This gave us 3,940 training samples and 985 test samples.

    \item \textbf{Variance Thresholding:} Two features had zero variance across the training set (they were always 0 for every patient after the dropout step). We removed them with \texttt{VarianceThreshold}, bringing the final feature count to 134.
\end{enumerate}

\subsection{Exploratory Data Analysis}

Before jumping to modelling, we spent time actually looking at the data.

\begin{itemize}
    \item \textbf{Class Distribution:} Each of the 41 disease classes has roughly 120 records, which is fairly balanced. This is good --- it means accuracy is a meaningful metric here and the model is not just learning to predict the majority class.

    \item \textbf{Symptom Frequency:} Symptoms like \textit{fatigue}, \textit{vomiting}, and \textit{high fever} appeared most frequently across the dataset, largely because they are associated with many different diseases. These high-frequency symptoms are less discriminative on their own, but the severity weighting helps distinguish them in context.

    \item \textbf{Correlation Heatmap:} Looking at the top 20 symptoms, we found noticeable correlations within disease-specific clusters. For example, symptoms like \textit{yellowing of eyes}, \textit{dark urine}, and \textit{loss of appetite} all tend to co-occur in hepatitis-related diseases. This is exactly the kind of structure a tree-based model can exploit.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{correlation_heatmap.png}
    \caption{Correlation Heatmap of Top 20 Symptoms}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{class_distribution.png}
    \caption{Disease Class Distribution}
\end{figure}

\subsection{Model Development}

\subsubsection{Decision Tree Classifier}
Quinlan (1986) first formalised the idea behind decision trees: at each node, pick the feature whose split best separates the remaining classes, repeat recursively, and stop when a leaf is pure (or nearly so). In our case, the Gini impurity criterion picks the symptom that best divides the disease classes at each step --- for example, ``is the severity of yellowing of eyes above 3?'' routes the sample toward hepatitis-related conditions.

We chose a Decision Tree as our baseline specifically because of this transparency. When a patient asks ``why does the system think I have malaria?'', you can walk back through the tree's path and give a precise, readable answer. We did not restrict the tree depth, so it could learn the full complexity of the training data --- which also means it overfits more, as we expected given the 50\% symptom dropout noise.

\textbf{Configuration:} \texttt{criterion='gini'}, \texttt{random\_state=42}, no depth limit (intentional, to establish an upper-complexity baseline).

\subsubsection{Random Forest Classifier}
Building on Breiman (2001), our Random Forest trains 100 decision trees, each on a different bootstrap sample of the training data and each restricted to a random subset of features at every split. The prediction is a majority vote across all 100 trees.

We picked 100 estimators because beyond that number the accuracy gain in our experiments was marginal, while training time scales linearly. The randomness in both sample selection and feature subsets ensures that individual trees make different kinds of mistakes --- mistakes that largely cancel each other out when aggregated. This is especially valuable here because the 50\% dropout step we applied artificially introduces random noise, and Random Forest's averaging mechanism is precisely designed to handle that.

\textbf{Configuration:} \texttt{n\_estimators=100}, \texttt{random\_state=42},\\
\hspace*{2.6cm}\texttt{n\_jobs=-1} (parallel training).

\subsubsection{XGBoost Classifier}
We included XGBoost (Chen \& Guestrin, 2016) because it handles the class overlap problem differently from Random Forest. Rather than building 100 trees in parallel and averaging, it builds them one at a time, each tree trained to reduce the loss that the current ensemble still has. A learning rate of 0.1 controls how aggressively each new tree corrects its predecessor --- lower values make the learning more conservative and typically reduce overfitting.

The \texttt{max\_depth=6} limit was chosen to prevent individual trees from becoming too specialised on noisy training samples, and \texttt{mlogloss} was used as the evaluation metric because it is designed for multi-class problems and penalises confident wrong predictions more than uncertain ones.

\textbf{Configuration:} \texttt{n\_estimators=100}, \texttt{learning\_rate=0.1},\\
\hspace*{2.6cm}\texttt{max\_depth=6}, \texttt{eval\_metric='mlogloss'}.

\subsection{Model Evaluation}

We evaluated each model on the held-out test set (985 samples) using the following metrics:
\begin{itemize}
    \item \textbf{Accuracy} --- the simple fraction of correctly predicted samples.
    \item \textbf{Precision} --- of all the times a model predicted Disease X, how often was it actually Disease X?
    \item \textbf{Recall} --- of all the actual cases of Disease X in the test set, how many did the model catch?
    \item \textbf{F1-Score} --- balances precision and recall into a single number; useful because both matter in medical settings.
    \item \textbf{Confusion Matrix} --- a full breakdown showing exactly which diseases are being confused with which. This is more informative than accuracy alone.
\end{itemize}

% ================================================================
\section{System Architecture}
% ================================================================

The full pipeline from raw files to final prediction is shown in Figure~\ref{fig:pipeline} below.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0.55cm and 1cm,
    startstop/.style={
        rectangle, rounded corners=6pt,
        minimum width=8cm, minimum height=0.65cm,
        text centered, draw=teal!70!black, fill=teal!15,
        font=\small\bfseries
    },
    process/.style={
        rectangle,
        minimum width=8cm, minimum height=0.65cm,
        text centered, draw=blue!50!black, fill=blue!8,
        font=\small
    },
    arrow/.style={thick, ->, >=Stealth, draw=gray!60!black}
]

\node (start)    [startstop]                        {START};
\node (load)     [process, below=of start]          {Load \texttt{disease\_raw.csv} \& \texttt{symptom\_severity.csv}};
\node (build)    [process, below=of load]           {Build 136-Dimensional Severity-Weighted Feature Matrix};
\node (dropout)  [process, below=of build]          {Apply 50\% Symptom Dropout (seed = 42)};
\node (encode)   [process, below=of dropout]        {Label Encode Target Column (\texttt{prognosis} $\to$ integer)};
\node (split)    [process, below=of encode]         {Stratified 80 / 20 Train--Test Split};
\node (variance) [process, below=of split]          {Remove Zero-Variance Features (VarianceThreshold)};
\node (train)    [process, below=of variance]       {Train: Decision Tree \quad Random Forest \quad XGBoost};
\node (predict)  [process, below=of train]          {Generate Predictions on Held-Out Test Set (985 samples)};
\node (evaluate) [process, below=of predict]        {Evaluate: Accuracy, Precision, Recall, F1, Confusion Matrix};
\node (end)      [startstop, below=of evaluate]     {OUTPUT: Predicted Disease Label};

\draw [arrow] (start)    -- (load);
\draw [arrow] (load)     -- (build);
\draw [arrow] (build)    -- (dropout);
\draw [arrow] (dropout)  -- (encode);
\draw [arrow] (encode)   -- (split);
\draw [arrow] (split)    -- (variance);
\draw [arrow] (variance) -- (train);
\draw [arrow] (train)    -- (predict);
\draw [arrow] (predict)  -- (evaluate);
\draw [arrow] (evaluate) -- (end);

\end{tikzpicture}
\caption{End-to-End System Pipeline Flowchart}
\label{fig:pipeline}
\end{figure}

Breaking it down into logical components:

\begin{itemize}
    \item \textbf{Input Layer:} Two CSV files --- \texttt{disease\_raw.csv} (patient records) and\\
          \texttt{symptom\_severity.csv} (severity weights).
    \item \textbf{Feature Builder:} Converts each patient record from a list of symptom names into a numerical severity vector.
    \item \textbf{Preprocessor:} Applies dropout simulation, encodes labels, filters zero-variance features, and splits the data.
    \item \textbf{Model Layer:} Three classifiers trained independently on the same training set.
    \item \textbf{Evaluation Layer:} Accuracy, per-class metrics, and confusion matrix plots.
    \item \textbf{Output:} A predicted disease class for each test sample.
\end{itemize}

% ================================================================
\section{Implementation}
% ================================================================

\subsection{Tools and Technologies}

\begin{table}[H]
\centering
\caption{Technologies Used}
\begin{tabular}{ll}
\toprule
\textbf{Tool / Library} & \textbf{Purpose} \\
\midrule
Python 3.9              & Core programming language \\
Pandas                  & Data loading and manipulation \\
NumPy                   & Numerical operations \\
Scikit-learn            & Preprocessing, DT, RF, evaluation \\
XGBoost                 & Gradient boosting classifier \\
Matplotlib / Seaborn    & Visualisation \\
Jupyter Notebook        & Interactive development environment \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Implementation Steps}

\begin{enumerate}
    \item Load \texttt{disease\_raw.csv} and \texttt{symptom\_severity.csv} from the \texttt{dataset/} subfolder.
    \item Build a 136-dimensional severity-weighted feature matrix for each patient.
    \item Randomly zero out 50\% of reported symptoms to simulate incomplete clinical data.
    \item Encode the \texttt{prognosis} column to integer class indices.
    \item Apply a stratified 80/20 train-test split.
    \item Drop zero-variance features from the training set (and apply the same mask to the test set).
    \item Train all three classifiers on the processed training data.
    \item Generate predictions on the test set and compute evaluation metrics.
    \item Plot confusion matrices and feature importances for analysis.
\end{enumerate}

\subsection{Key Code Snippet}

\begin{lstlisting}[caption=Severity-Weighted Feature Construction]
# Build severity lookup
sev_dict = {row['Symptom'].strip(): int(row['weight'])
            for _, row in sev_df.iterrows()}

# Build feature matrix
X_sev = pd.DataFrame(0.0, index=df_raw.index, columns=all_symptoms)
for col in symptom_cols:
    for idx, val in df_raw[col].items():
        s = str(val).strip()
        if s in all_symptoms:
            X_sev.loc[idx, s] = float(sev_dict.get(s, 1))
\end{lstlisting}

\begin{lstlisting}[caption=Model Training and Evaluation]
# Random Forest
rf_model = RandomForestClassifier(
    n_estimators=100, random_state=42, n_jobs=-1)
rf_model.fit(X_train_sel, y_train)
rf_pred = rf_model.predict(X_test_sel)
rf_acc  = accuracy_score(y_test, rf_pred)
print(f'Random Forest Accuracy: {rf_acc * 100:.2f}%')
\end{lstlisting}

% ================================================================
\section{Results}
% ================================================================

\subsection{Model Accuracy Comparison}

\begin{table}[H]
\centering
\caption{Model Performance on Test Set (985 samples)}
\begin{tabular}{lccc}
\toprule
\textbf{Model} & \textbf{Accuracy} & \textbf{Macro Precision} & \textbf{Macro Recall} \\
\midrule
Decision Tree  & 87.72\% & 0.90 & 0.88 \\
Random Forest  & 91.88\% & 0.94 & 0.92 \\
XGBoost        & 91.07\% & 0.93 & 0.91 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{model_comparison.png}
    \caption{Accuracy Comparison of Classification Models}
\end{figure}

\subsection{Confusion Matrices}

The confusion matrices below are normalised row-wise, so each cell shows the fraction of actual instances of a class that were predicted as each possible class. A perfect classifier would show 1.00 along the diagonal and 0.00 everywhere else.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{cm_decision_tree.png}
    \caption{Confusion Matrix -- Decision Tree (Normalised)}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{cm_random_forest.png}
    \caption{Confusion Matrix -- Random Forest (Normalised)}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{cm_xgboost.png}
    \caption{Confusion Matrix -- XGBoost (Normalised)}
\end{figure}

\subsection{Feature Importance}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{feature_importance.png}
    \caption{Top 20 Feature Importances -- Random Forest}
\end{figure}

The Random Forest model identified symptoms like \textit{yellowing of eyes}, \textit{dark urine}, \textit{sweating}, and \textit{chills} as the most discriminative across the 41 classes. This is clinically sensible --- these symptoms are strongly associated with specific diseases (e.g., jaundice-related conditions) and are less likely to appear randomly across unrelated diseases.

% ================================================================
\section{Discussion}
% ================================================================

\subsection{What the Numbers Actually Mean}

The accuracy gap between models is small in absolute terms --- just 4 percentage points between the best and worst --- but the consistency across precision and recall tells an interesting story.

\textbf{Random Forest at 91.88\%} was the strongest performer. Looking at the confusion matrix, it handles most disease classes cleanly. The classes where it struggles are predictably the hepatitis variants (B, C, D, E), which share so many symptoms that even a human doctor would want lab tests before deciding. The ensemble averaging smooths out the noise introduced by the 50\% dropout.

\textbf{XGBoost at 91.07\%} was essentially neck-and-neck with Random Forest. On this dataset, the sequential boosting approach did not offer a meaningful edge over bagging. This is probably because the dominant source of error here is symptom overlap between classes, not residual fitting errors that boosting is designed to correct.

\textbf{Decision Tree at 87.72\%} still performed well, which is worth noting. A single tree is fast, transparent, and easy to deploy. If interpretability is the primary concern, 87.72\% with a readable decision path may actually be preferable to 91.88\% from a black-box ensemble.

\subsection{What Works Well and What Does Not}

On the positive side, the severity-weighted features gave us a noticeable advantage over simple binary encoding, and the dropout simulation made the models more robust to missing inputs at test time. The balanced class distribution also meant we did not have to worry about the model being biassed towards common diseases.

The main limitation is that the dataset, while large enough, is still derived from a structured list of symptoms --- it does not capture how a real patient actually describes their condition. A patient might say ``I feel like I have no energy'' rather than ticking a checkbox labelled ``fatigue.'' Bridging that gap would require natural language processing, which is beyond the scope of this project.

% ================================================================
\section{Applications}
% ================================================================

\begin{itemize}
    \item \textbf{General Practice Clinics:} A GP seeing 30 patients a day could run each patient's reported symptoms through the model as a quick second opinion --- not to replace the diagnosis, but to flag cases where the symptom pattern matches something the doctor might not have considered first.
    \item \textbf{Low-Connectivity Remote Areas:} A lightweight version of the model (the Decision Tree alone is tiny) could run offline on a basic Android phone, giving community health workers in villages a tool to decide whether a patient needs to travel to a specialist or can be treated locally.
    \item \textbf{Triage in Outpatient Queues:} Before a patient sees a doctor, a nurse could enter their symptoms and get a ranked shortlist of likely conditions. The doctor then starts the consultation already oriented toward the probable diagnoses, which saves time and reduces the chance of a missed diagnosis under time pressure.
    \item \textbf{Personal Symptom Checking:} A web or mobile form backed by the model could help people answer the question ``should I go to a hospital or wait and see?'' --- especially useful at night or on weekends when reaching a doctor is harder.
    \item \textbf{Teaching Aid in Medical Colleges:} The feature importance chart is genuinely useful in a classroom: it shows students which symptoms are most discriminative across diseases, which reinforces the clinical reasoning behind symptom clusters far better than a static textbook table.
\end{itemize}

% ================================================================
\section{Future Scope}
% ================================================================

There are several ways this project could be meaningfully extended:

\begin{itemize}
    \item \textbf{Hyperparameter Tuning:} We used default or sensible parameters, but a systematic search using \texttt{GridSearchCV} or \texttt{Optuna} could squeeze out additional accuracy, particularly for XGBoost.
    \item \textbf{Neural Networks:} A simple Multi-Layer Perceptron would be a natural next step. Deeper architectures might better capture non-linear interactions between symptoms.
    \item \textbf{Explainability with SHAP:} Right now, we know which symptoms are globally important. SHAP values would let us explain individual predictions --- ``the model predicted malaria primarily because of high fever combined with chills'' --- which is far more useful in a clinical context.
    \item \textbf{Web Deployment:} Wrapping the best model in a Flask or FastAPI backend and building a simple symptom-input form would make this a usable tool, not just a research prototype.
    \item \textbf{Richer Patient Data:} Adding demographic features (age, gender), vital signs (blood pressure, temperature), and basic lab values would substantially improve real-world prediction quality.
\end{itemize}

% ================================================================
\section{Conclusion}
% ================================================================

We set out to build a system that could predict diseases from symptoms, and the results are genuinely encouraging. All three models performed well above chance on a challenging 41-class problem:

\begin{itemize}
    \item Decision Tree: \textbf{87.72\%}
    \item XGBoost: \textbf{91.07\%}
    \item Random Forest: \textbf{91.88\%} $\leftarrow$ best overall
\end{itemize}

The severity-weighted feature representation was central to these results --- it gave the models richer information than a binary symptom checklist would have. The symptom dropout step made the training more realistic and improved the models' ability to handle incomplete inputs.

Random Forest is our recommended model for any deployment scenario. It is robust, accurate, and interpretable enough to understand through feature importance analysis. More broadly, this project reinforces the idea that well-engineered classical machine learning is still highly competitive for structured medical data and does not require deep learning to produce clinically meaningful results.

% ================================================================
\section{Roles and Contributions}
% ================================================================

\begin{table}[H]
\centering
\caption{Team Contributions}
\small
\begin{tabular}{|c|p{3.5cm}|p{7.8cm}|}
\hline
\textbf{Candidate} & \textbf{Name} & \textbf{Contribution} \\
\hline
A & NAME 1 & Dataset collection, severity-weighted feature construction, data preprocessing \\
\hline
B & NAME 2 & Model development -- Decision Tree, Random Forest, XGBoost training and tuning \\
\hline
C & NAME 3 & Model evaluation, confusion matrix plots, feature importance analysis \\
\hline
D & NAME 4 & Documentation, LaTeX report writing, presentation preparation \\
\hline
\end{tabular}
\end{table}

\vspace{0.4cm}
Beyond the above division of work, all four members sat together for every key discussion, helped each other debug, and reviewed the final output as a group.

% ================================================================
\section{References}
% ================================================================

\begin{enumerate}
    \item Breiman, L. (2001). Random Forests. \textit{Machine Learning}, 45(1), 5--32.
    \item Chen, T., \& Guestrin, C. (2016). XGBoost: A Scalable Tree Boosting System. \textit{Proceedings of the 22nd ACM SIGKDD International Conference on Knowledge Discovery and Data Mining}, 785--794.
    \item Kononenko, I. (2001). Machine learning for medical diagnosis: History, state of the art and perspective. \textit{Artificial Intelligence in Medicine}, 23(1), 89--109.
    \item Quinlan, J. R. (1986). Induction of Decision Trees. \textit{Machine Learning}, 1(1), 81--106.
    \item Rajpurkar, P., Irvin, J., Ball, R. L., et al. (2017). CheXNet: Radiologist-Level Pneumonia\\
          Detection on Chest X-Rays with Deep Learning. \textit{arXiv preprint arXiv:1711.05225}.
    \item itachi9604. (2020). \textit{Disease Symptom Prediction Dataset}. Kaggle.
          \newline\footnotesize\texttt{kaggle.com/datasets/itachi9604/}\\\hspace*{1em}\texttt{disease-symptom-description-dataset}\normalsize
    \item amMistic. (2023). \textit{Diseases-Prediction-based-on-Symptoms}. GitHub.
          \newline\footnotesize\texttt{github.com/amMistic/Diseases-Prediction-based-on-Symptoms}\normalsize
    \item Pedregosa, F., et al. (2011). Scikit-learn: Machine Learning in Python. \textit{Journal of Machine Learning Research}, 12, 2825--2830.
\end{enumerate}

% ================================================================
\section{Appendix}
% ================================================================

\subsection{Sample Dataset Records}

The table below shows a few rows from the processed severity-weighted feature matrix to illustrate the data format:

\begin{table}[H]
\centering
\caption{Sample Records from Processed Dataset}
\begin{tabular}{lccccl}
\toprule
\textbf{itching} & \textbf{skin\_rash} & \textbf{fever} & \textbf{fatigue} & \textbf{...} & \textbf{prognosis} \\
\midrule
1 & 3 & 0 & 5 & ... & Fungal infection \\
0 & 0 & 4 & 5 & ... & Common Cold \\
7 & 0 & 6 & 5 & ... & Dengue \\
0 & 0 & 0 & 0 & ... & Diabetes \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Full Preprocessing Code}

\begin{lstlisting}[caption=Complete Preprocessing Pipeline]
# Symptom dropout: zero out 50% of reported symptoms
rng = np.random.RandomState(42)
X_vals = X_raw.values.copy().astype(float)
non_zero_mask = X_vals > 0
drop_mask     = rng.random(X_vals.shape) < 0.50
X_vals[non_zero_mask & drop_mask] = 0.0
X = pd.DataFrame(X_vals, columns=X_raw.columns)

# Label encoding
le = LabelEncoder()
y_encoded = le.fit_transform(y_raw)

# Stratified train-test split
X_train, X_test, y_train, y_test = train_test_split(
    X, y_encoded, test_size=0.20,
    random_state=42, stratify=y_encoded)

# Remove zero-variance features
selector = VarianceThreshold(threshold=0.0)
selector.fit(X_train)
X_train_sel = selector.transform(X_train)
X_test_sel  = selector.transform(X_test)
\end{lstlisting}

\end{document}
